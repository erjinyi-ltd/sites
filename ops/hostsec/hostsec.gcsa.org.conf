# HostSec - hostsec.gcsa.org
# TLS terminates on the upstream Nginx proxy. This origin serves the introduction
# site from /var/www/hostsec and forwards HostSec control-plane routes to 9501.

# HTML must never leave an old asset manifest in browsers or intermediary CDNs.
# Vite's content-hashed /assets/ files are safe to cache permanently; stable-name
# public files are stored only with mandatory revalidation.
map "$uri|$sent_http_content_type" $hostsec_gcsa_cache_control {
    default "";
    ~^/(?:.*?/)?assets/ "public, max-age=31536000, immutable";
    ~[|]text/html "no-store, no-cache, must-revalidate, proxy-revalidate, max-age=0, s-maxage=0";
    ~^/.*\.(?:css|js|mjs|json|map|png|jpg|jpeg|gif|webp|avif|svg|ico|woff|woff2|ttf|otf|xml|txt)[|] "no-cache, must-revalidate";
}

map $sent_http_content_type $hostsec_gcsa_cdn_cache_control {
    default "";
    ~text/html "no-store";
}

map $sent_http_content_type $hostsec_gcsa_x_accel_expires {
    default "";
    ~text/html "0";
}

server {
    listen 80;
    listen [::]:80;
    server_name hostsec.gcsa.org;

    root /var/www/hostsec;
    index index.html;
    charset utf-8;
    client_max_body_size 64m;

    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml application/javascript application/json application/manifest+json application/rss+xml application/xml image/svg+xml;

    access_log /var/log/nginx/hostsec-gcsa-access.log combined;
    error_log  /var/log/nginx/hostsec-gcsa-error.log warn;

    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Strict-Transport-Security "max-age=31536000" always;
    add_header Cache-Control $hostsec_gcsa_cache_control always;
    add_header CDN-Cache-Control $hostsec_gcsa_cdn_cache_control always;
    add_header Cloudflare-CDN-Cache-Control $hostsec_gcsa_cdn_cache_control always;
    add_header X-Accel-Expires $hostsec_gcsa_x_accel_expires always;

    location = /admin {
        return 308 /admin/;
    }

    location = /tenant {
        return 308 /tenant/;
    }

    # HostSec control-plane shared images use absolute /img/... URLs.
    location ^~ /img/ {
        proxy_pass http://127.0.0.1:9501;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port 443;
    }

    location ^~ /admin/ {
        proxy_pass http://127.0.0.1:9501;
        proxy_http_version 1.1;
        proxy_buffering off;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port 443;
    }

    location ^~ /tenant/ {
        proxy_pass http://127.0.0.1:9501;
        proxy_http_version 1.1;
        proxy_buffering off;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port 443;
    }

    location ^~ /agent/ {
        proxy_pass http://127.0.0.1:9501;
        proxy_http_version 1.1;
        proxy_buffering off;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port 443;
    }

    location ^~ /tenant-preview/ {
        proxy_pass http://127.0.0.1:9501;
        proxy_http_version 1.1;
        proxy_buffering off;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port 443;
    }

    location ^~ /downloads/ {
        proxy_pass http://127.0.0.1:9501;
        proxy_http_version 1.1;
        proxy_buffering off;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port 443;
    }

    location ^~ /installers/ {
        proxy_pass http://127.0.0.1:9501;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port 443;
    }

    location = /health {
        proxy_pass http://127.0.0.1:9501;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port 443;
    }

    location = /metrics {
        proxy_pass http://127.0.0.1:9501;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port 443;
    }

    location ^~ /assets/ {
        try_files $uri =404;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }

    location ~* \.(?:css|js|mjs|json|map|png|jpg|jpeg|gif|webp|avif|svg|ico|woff2?|ttf|otf|xml|txt)$ {
        try_files $uri =404;
    }

    location = /index.html {
        try_files $uri =404;
    }

    location ^~ /server/ {
        deny all;
    }

    location ~ /\. {
        deny all;
    }
}
